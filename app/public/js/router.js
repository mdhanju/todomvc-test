define([
    'jquery',
    'underscore',
    'backbone'
], function($, _, Backbone) {
    "use strict";
    var router = {
        initialize: function(res) {
            return "Router Initialize Called" + res;
        }
    }
    return router;
});
